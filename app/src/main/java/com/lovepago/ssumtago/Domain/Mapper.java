package com.lovepago.ssumtago.Domain;

/**
 * Created by ParkHaeSung on 2017-05-26.
 */

public class Mapper {
   /* static class DTOtoRealm{
        public static Survey getSurvey(SurveyDTO surveyDTO){
            Survey survey = new Survey();
            survey.setDesc(surveyDTO.getDesc());
            survey.setId(surveyDTO.getId());
            for(String a : surveyDTO.getName()){
                survey.getName().add(new RealmString(a));
            }
            for(Question q : surveyDTO.getQuestions()){
                survey.getQuestions().add(q);
            }
            return
        }
    }*/
}
